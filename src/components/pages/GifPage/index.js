import React from 'react';
import GifPreview from '../../common/GifPreview';

const GifPage = () => {
    return (
        <div>
            <GifPreview/>
        </div>
    )
};

export default GifPage